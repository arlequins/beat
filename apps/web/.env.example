## Public URL of the separately hosted Beat chat app. This is compiled into the
## static portfolio bundle and must never contain a secret or API token.
# NEXT_PUBLIC_BEAT_APP_URL=http://localhost:3000
